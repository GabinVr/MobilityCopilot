coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: auto
        threshold: 5%

comment:
  layout: "reach, diff, flags, files"
  behavior: default

ignore:
  - "tests/"
  - "**/__pycache__/"
  - "**/test_*.py"
