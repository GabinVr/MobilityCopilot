{
  "name": "mobilitycerulot-web",
  "version": "1.0.0",
  "description": "Web interface for MobilityCopilot - BETH Stack",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun run --bun esbuild src/index.ts --bundle --platform=node --target=es2020 --outfile=dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "@elysiajs/html": "latest",
    "@elysiajs/static": "latest",
    "@supabase/supabase-js": "latest",
    "elysia": "latest",
    "esbuild": "^0.27.3",
    "htmx.org": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "latest",
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
